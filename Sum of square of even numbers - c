#include <stdio.h>
int main() {
   int n,sum=0;
   printf("enter the number: ");
   scanf("%d",&n);
   for (int i = 1; i <= n; i++){
      sum += (2*i) * (2*i);
    }
   printf("The sum of square of first %d even numbers is %d",n, sum);
   return 0;
}
